.search_wrapper {
  display: flex;
  align-items: center;
  height: 5rem;
  margin: 0 2rem;

  .search {
    height: 100%;
    width: 100%;
    border: 0;
    font-size: 2rem;
    font-weight: 200;
  }

  .add_icon {
    cursor: pointer;
  }
}
