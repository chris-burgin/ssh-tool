.search_wrapper {
  display: flex;
  align-items: center;
  height: 5rem;
  padding: 0 2rem;
  position: fixed;
  background-color: white;
  width: 100%;

  .search {
    height: 100%;
    width: 90%;
    border: 0;
    font-size: 2rem;
    font-weight: 200;
  }

  .add_icon {
    cursor: pointer;
  }
}
